{
    "name": "badges",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "deploy": "tsc && wrangler deploy",
        "dev": "wrangler dev",
        "types": "wrangler types ./types/wrangler.d.ts"
    },
    "devDependencies": {
        "typescript": "^5.9.3",
        "wrangler": "^4.47.0"
    },
    "dependencies": {
        "@adonix.org/cloud-spark": "^0.0.187",
        "badgen": "^3.2.3"
    }
}
